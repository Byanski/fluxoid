<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  build/entitlements.mac.plist
  Required for Electron apps distributed outside the Mac App Store when
  hardenedRuntime: true is set in electron-builder.config.js.

  Place this file at:  fluxer_desktop/build/entitlements.mac.plist
-->
<plist version="1.0">
  <dict>
    <!-- Allow the JIT compiler Electron's V8 needs -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>

    <!-- Allow unsigned executable memory (needed by some native modules) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>

    <!-- Allow DYLD environment variables (needed by Electron's sandbox helper) -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>

    <!-- Network access (outbound HTTPS / WSS to fluxer.app) -->
    <key>com.apple.security.network.client</key>
    <true/>

    <!-- Microphone — only if Fluxer uses voice chat -->
    <key>com.apple.security.device.audio-input</key>
    <true/>

    <!-- Camera — only if Fluxer uses video -->
    <key>com.apple.security.device.camera</key>
    <true/>
  </dict>
</plist>
